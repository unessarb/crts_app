




{% extends 'layouts/base.html.twig' %}

{% block title 'Liste des natures de recette' %}

{% block body %}

	<!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>Liste des natures de recette</h1>
          </div>
          <div class="col-sm-6 text-right">
            <a role="link" href="{{path('nature_recette_new')}}" type="button" class="btn btn-primary "><i class="fa fa-plus"></i> Nouvelle nature recette</a>
          </div>
        </div>
      </div><!-- /.container-fluid -->
    </section>

     <!-- Main content -->
    <section class="content">
      <div class="container-fluid">
        <div class="row">
          <div class="col-12">
            <div class="card">
              <div class="card-body">
                <table id="example2" class="table table-bordered table-hover">
                  <thead>
                  <tr>
                    <th>#</th>
                <th>Identifiant</th>
                <th>Code</th>
                <th>Nature recette</th>
                <th>actions</th>
                  </tr>
                  </thead>
                  <tbody>
                    {% for item in nature_recettes %}
                        <tr>
                            <td >{{ loop.index }}</td>          
                            <td>{{item.id}}</td>                        
                            <td>{{item.code}}</td>                        
                            <td>{{item.natureOperation}}</td>                        
                                                  
                            <td>
                                <a class="btn btn-info btn-sm" 
                                data-toggle="tooltip" data-placement="top" title="Editer"
                                href="{{ path('nature_recette_edit', {id: item.id}) }}">
                                    <i class="fas fa-pencil-alt"></i>
                                    Edit
                                </a>  
                                <a href="#" 
                                data-toggle="tooltip" data-placement="top" title="Supprimer"
                                onclick="event.preventDefault(); confirm('Êtes-vous sûre') && document.getElementById('js-nature_recette-delete-form').submit();" 
                                class="btn btn-danger btn-sm">
                                    <i class="fas fa-trash"></i>
                                    Supprimer
                                </a>

                                <form id="js-nature_recette-delete-form" action="{{ path('nature_recette_delete', {id: item.id}) }}" method="post" style="display: none;">
                                    <input type="hidden" name="csrf_token" value="{{ csrf_token('nature_recette_deletion_' ~ item.id) }}">
                                    <input type="hidden" name="_method" value="DELETE">
                                </form> 
                            </td>                        
                        </tr>
                    {% endfor %}
                  </tbody>
                </table>
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
          </div>
          <!-- /.col -->
        </div>
        <!-- /.row -->
      </div>
      <!-- /.container-fluid -->
    </section>
    <!-- /.content -->


{% endblock %}
