<!DOCTYPE html>

<title>Recette</title>

{% block body %}
    <h1>Recette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette.id }}</td>
            </tr>
            <tr>
                <th>NumCC</th>
                <td>{{ recette.numCC }}</td>
            </tr>
            <tr>
                <th>Partieversantes</th>
                <td>{{ recette.partieversantes }}</td>
            </tr>
            <tr>
                <th>Obs</th>
                <td>{{ recette.obs }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ recette.montant }}</td>
            </tr>
            <tr>
                <th>DocumentPassation</th>
                <td>{{ recette.documentPassation }}</td>
            </tr>
            <tr>
                <th>DocumentExecution</th>
                <td>{{ recette.documentExecution }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ recette.createdAt ? recette.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ recette.updatedAt ? recette.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recette_index') }}">back to list</a>

    <a href="{{ path('recette_edit', {'id': recette.id}) }}">edit</a>

    {{ include('recette/_delete_form.html.twig') }}
{% endblock %}
