{% extends 'layouts/base.html.twig' %}

{% block title 'Tableau de bord' %}

{% block body %}

<!-- Content Header (Page header) -->
<div class="content-header">
	<div class="container-fluid">
	<div class="row mb-2">
		<div class="col-sm-6">
			<h1 class="m-0">Tableau de bord</h1>
		</div><!-- /.col -->
		
	</div><!-- /.row -->
	</div><!-- /.container-fluid -->
</div>
<!-- /.content-header -->

<!-- Main content -->
<section class="content">
	<div class="container-fluid">
	{% if is_granted('ROLE_REGIE') %}  

	<!-- Small boxes (Stat box) -->
		<div class="row">
			
			<div class="col-lg-3 col-6">
				<!-- small box -->
				<div class="small-box bg-success">
					<div class="inner">
					<h3>{{ countD }}</h3>

					<p>Dépenses</p>
					</div>
					<div class="icon">
					<i class="ion ion-stats-bars"></i>
					</div>
					<a href="{{ path('app_depenses') }}" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>
				</div>
			</div>
			<!-- ./col -->
			<div class="col-lg-3 col-6">
				<!-- small box -->
				<div class="small-box bg-warning">
					<div class="inner">
					<h3>{{ countR }}</h3>

					<p>Recettes</p>
					</div>
					<div class="icon">
					<i class="ion ion-person-add"></i>
					</div>
					<a href="{{ path('recette_index') }}" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>
				</div>
			</div>
			<!-- ./col -->
			<div class="col-lg-3 col-6">
				<!-- small box -->
				<div class="small-box bg-danger">
					<div class="inner">
					<h3>{{ countNatureD }}</h3>

					<p>Rubriques</p>
					</div>
					<div class="icon">
					<i class="ion ion-pie-graph"></i>
					</div>
					<a href="{{ path('app_nature_operation_depense') }}" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>
				</div>
			</div>
			<!-- ./col -->
			<div class="col-lg-3 col-6">
				<!-- small box -->
				<div class="small-box bg-info">
					<div class="inner">
					<h3>{{ countNatureR }}</h3>

					<p>Natures Recette</p>
					</div>
					<div class="icon">
					<i class="ion ion-bag"></i>
					</div>
					{% if is_granted('ROLE_ADMIN') %}
						<a href="{{ path('app_snature_recette_indexocietes') }}" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>
					{% else %}
						<a href="#" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>

					{% endif %}
				</div>
			</div>
			<!-- ./col -->
		</div>
		<!-- /.row -->

	{% else %} 
		<!-- Small boxes (Stat box) -->
		<div class="row">
			
			<div class="col-lg-3 col-6">
				<!-- small box -->
				<div class="small-box bg-success">
					<div class="inner">
					<h3>{{ countMarche }}</h3>

					<p>Marchés Unique / Cadre</p>
					</div>
					<div class="icon">
					<i class="ion ion-stats-bars"></i>
					</div>
					<a href="{{ path('app_marches') }}" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>
				</div>
			</div>
			<!-- ./col -->
			<div class="col-lg-3 col-6">
				<!-- small box -->
				<div class="small-box bg-warning">
					<div class="inner">
					<h3>{{ countBonCmd }}</h3>

					<p>Bon Commandes</p>
					</div>
					<div class="icon">
					<i class="ion ion-person-add"></i>
					</div>
					<a href="{{ path('app_bon_commandes') }}" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>
				</div>
			</div>
			<!-- ./col -->
			<div class="col-lg-3 col-6">
				<!-- small box -->
				<div class="small-box bg-danger">
					<div class="inner">
					<h3>{{ countContrat }}</h3>

					<p>Contrats - Conventions</p>
					</div>
					<div class="icon">
					<i class="ion ion-pie-graph"></i>
					</div>
					<a href="{{ path('app_contrats') }}" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>
				</div>
			</div>
			<!-- ./col -->
			<div class="col-lg-3 col-6">
				<!-- small box -->
				<div class="small-box bg-info">
					<div class="inner">
					<h3>{{ countSociete }}</h3>

					<p>Nombre de sociétés</p>
					</div>
					<div class="icon">
					<i class="ion ion-bag"></i>
					</div>
					{% if is_granted('ROLE_ADMIN') %}
						<a href="{{ path('app_societes') }}" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>
					{% else %}
						<a href="#" class="small-box-footer">Plus d'infos <i class="fas fa-arrow-circle-right"></i></a>

					{% endif %}
				</div>
			</div>
			<!-- ./col -->
		</div>
		<!-- /.row -->
{% endif %}  

{% if is_granted('ROLE_REGIE') %} 

<div class="row mb-2">
	
		<div class="col-sm-6">


		         <!-- DONUT CHART -->
            <div class="card card-primary">
              <div class="card-header">
			
                <h3 class="card-title">Nombre des Dépenses/Recettes</h3>

                <div class="card-tools">
                  <button type="button" class="btn btn-tool" data-card-widget="collapse">
                    <i class="fas fa-minus"></i>
                  </button>
                  <button type="button" class="btn btn-tool" data-card-widget="remove">
                    <i class="fas fa-times"></i>
                  </button>
                </div>
              </div>
              <div class="card-body">
			  <center>
			  		<select id="select1" class="select2bs4" tabindex="-1" aria-hidden="true" style="width:300px;">
		<option value="" selected="selected">Selectionner l'année</option>
		{% for i in 2000..2040 %}
		{% if dateM==i %}

		<option value="{{i}}" selected>{{i}}</option><br>
		{% else %}

<option value="{{i}}">{{i}}</option><br>
{% endif %}
{% endfor %}

		</select>
		</center>
		
                <canvas id="barChart" style="min-height: 250px; height: 250px; max-height: 250px; max-width: 100%;"></canvas>
                </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
			</div>
			<!-- ./col -->


		
		<div class="col-sm-6">


		         <!-- DONUT CHART -->
            <div class="card card-info">
              <div class="card-header">
			
                <h3 class="card-title">Montant des Dépenses/Recettes (MAD)</h3>

                <div class="card-tools">
                  <button type="button" class="btn btn-tool" data-card-widget="collapse">
                    <i class="fas fa-minus"></i>
                  </button>
                  <button type="button" class="btn btn-tool" data-card-widget="remove">
                    <i class="fas fa-times"></i>
                  </button>
                </div>
              </div>
              <div class="card-body">
			  <center>
			  		<select id="select2" class="select2bs4" tabindex="-1" aria-hidden="true" style="width:300px;">
		<option value="" selected="selected">Selectionner l'année</option>
		{% for i in 2000..2040 %}
		{% if dateM==i %}

		<option value="{{i}}" selected>{{i}}</option><br>
		{% else %}

<option value="{{i}}">{{i}}</option><br>
{% endif %}
{% endfor %}

		</select>
		</center>
		
                <canvas id="barChart2" style="min-height: 250px; height: 250px; max-height: 250px; max-width: 100%;"></canvas>
                </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
			</div>
			<!-- ./col -->









		</div>
		<!-- /.row -->

{% else %} 

	<div class="row mb-2">
			<div class="col-sm-2">
			</div>
		<div class="col-sm-8">


		         <!-- DONUT CHART -->
            <div class="card card-danger">
              <div class="card-header">
			
                <h3 class="card-title">Répartition dans l'année</h3>

                <div class="card-tools">
                  <button type="button" class="btn btn-tool" data-card-widget="collapse">
                    <i class="fas fa-minus"></i>
                  </button>
                  <button type="button" class="btn btn-tool" data-card-widget="remove">
                    <i class="fas fa-times"></i>
                  </button>
                </div>
              </div>
              <div class="card-body">
			  <center>
			  		<select id="marche_unique_tfs" class="select2bs4" tabindex="-1" aria-hidden="true" style="width:300px;">
		<option value="" selected="selected">Selectionner l'année</option>
		{% for i in 2000..2040 %}
		{% if dateM==i %}

		<option value="{{i}}" selected>{{i}}</option><br>
		{% else %}

<option value="{{i}}">{{i}}</option><br>
{% endif %}
{% endfor %}

		</select>
		</center>
		
                <canvas id="donutChart" style="min-height: 250px; height: 250px; max-height: 250px; max-width: 100%;"></canvas>
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
			</div>
			<!-- ./col -->
		</div>
		<!-- /.row -->

{% endif %} 

	










	</div><!-- /.container-fluid -->

</section>
<!-- /.content -->





{% endblock %}

{% block javascript %}
<script>
$("#marche_unique_tfs").on('change', function() {
  //alert( this.value );

$.ajax('/graphique/'+this.value, {
    type: 'GET',  // http method
    success: function (data, status, xhr) {
        console.log(data);
		console.log(donutData.datasets);
		donutData.datasets[0]['data']=data;
		chart.update();
		
    },
    error: function (jqXhr, textStatus, errorMessage) {
            console.log('Error' + errorMessage);
    }
});
});

</script>

<script>
 //-------------
    //- DONUT CHART -
    //-------------
    // Get context with jQuery - using jQuery's .get() method.
    var donutChartCanvas = $('#donutChart').get(0).getContext('2d')
    var donutData        = {
      labels: [
          'Marchés',
          'Bon de Commande',
          'CDC/Convention',
      ],
      datasets: [
        {
          data: [700,500,400],
          backgroundColor : ['#f56954', '#00a65a', '#f39c12', '#00c0ef', '#3c8dbc', '#d2d6de'],
        }
      ]
    }
    var donutOptions     = {
      maintainAspectRatio : false,
      responsive : true,
    }
    //Create pie or douhnut chart
    // You can switch between pie and douhnut using the method below.
    var chart=new Chart(donutChartCanvas, {
      type: 'doughnut',
      data: donutData,
      options: donutOptions
    })


</script>

    <script>
        $(document).ready(function() {
 			$.ajax('/graphique/{{dateM}}', {
    			type: 'GET',  // http method
    			success: function (data, status, xhr) {
       			console.log(data);
				//console.log(donutData.datasets);
				donutData.datasets[0]['data']=data;
				chart.update();
		
    		},
    			error: function (jqXhr, textStatus, errorMessage) {
            	console.log('Error' + errorMessage);
    }
});

$.ajax('/graphique2/{{dateM}}', {
    type: 'GET',  // http method
    success: function (data, status, xhr) {
        console.log(data);
		console.log(areaChartData.datasets);
		areaChartData.datasets[0]['data']=data[0];
		//chart1.update();

		areaChartData.datasets[1]['data']=data[1];
		chart1.update();


		areaChartData2.datasets[0]['data']=data[2];
		//chart1.update();

		areaChartData2.datasets[1]['data']=data[3];
		chart2.update();
		
    },
    error: function (jqXhr, textStatus, errorMessage) {
            console.log('Error' + errorMessage);
    }
});
        });
    </script>


	<script>

    var areaChartData = {
      labels  : ['Janvier', 'Février', 'Mars', 'Avril', 'Mai', 'Juin', 'Juillet','Aout','Septembre','Octobre','Novembre','Décembre'],
      datasets: [
        {
          label               : 'Dépenses',
          backgroundColor     : 'rgba(60,141,188,0.9)',
          borderColor         : 'rgba(60,141,188,0.8)',
          pointRadius          : false,
          pointColor          : '#3b8bba',
          pointStrokeColor    : 'rgba(60,141,188,1)',
          pointHighlightFill  : '#fff',
          pointHighlightStroke: 'rgba(60,141,188,1)',
          data                : [28, 48, 40, 19, 86, 27, 90,28, 48, 40, 19, 86, 27, 90]
        },
        {
          label               : 'Recettes',
          backgroundColor     : 'rgba(210, 214, 222, 1)',
          borderColor         : 'rgba(210, 214, 222, 1)',
          pointRadius         : false,
          pointColor          : 'rgba(210, 214, 222, 1)',
          pointStrokeColor    : '#c1c7d1',
          pointHighlightFill  : '#fff',
          pointHighlightStroke: 'rgba(220,220,220,1)',
          data                : [65, 59, 80, 81, 56, 55, 40,28, 48, 40, 19, 86, 27, 90]
        },
      ]
    }

	    var areaChartData2 = {
      labels  : ['Janvier', 'Février', 'Mars', 'Avril', 'Mai', 'Juin', 'Juillet','Aout','Septembre','Octobre','Novembre','Décembre'],
      datasets: [
        {
          label               : 'Dépenses',
          backgroundColor     : 'rgba(60,141,188,0.9)',
          borderColor         : 'rgba(60,141,188,0.8)',
          pointRadius          : false,
          pointColor          : '#3b8bba',
          pointStrokeColor    : 'rgba(60,141,188,1)',
          pointHighlightFill  : '#fff',
          pointHighlightStroke: 'rgba(60,141,188,1)',
          data                : [28, 48, 40, 19, 86, 27, 90,28, 48, 40, 19, 86, 27, 90]
        },
        {
          label               : 'Recettes',
          backgroundColor     : 'rgba(210, 214, 222, 1)',
          borderColor         : 'rgba(210, 214, 222, 1)',
          pointRadius         : false,
          pointColor          : 'rgba(210, 214, 222, 1)',
          pointStrokeColor    : '#c1c7d1',
          pointHighlightFill  : '#fff',
          pointHighlightStroke: 'rgba(220,220,220,1)',
          data                : [65, 59, 80, 81, 56, 55, 40,28, 48, 40, 19, 86, 27, 90]
        },
      ]
    }
    //-------------
    //- BAR CHART -
    //-------------
    var barChartCanvas = $('#barChart').get(0).getContext('2d')
    var barChartData = $.extend(true, {}, areaChartData)
    var temp0 = areaChartData.datasets[0]
    var temp1 = areaChartData.datasets[1]
    barChartData.datasets[0] = temp1
    barChartData.datasets[1] = temp0

    var barChartOptions = {
      responsive              : true,
      maintainAspectRatio     : false,
      datasetFill             : false
    }

    var chart1=new Chart(barChartCanvas, {
      type: 'bar',
      data: barChartData,
      options: barChartOptions
    })



    //-------------
    //- BAR CHART2 -
    //-------------
    var barChartCanvas2 = $('#barChart2').get(0).getContext('2d')
    var barChartData2 = $.extend(true, {}, areaChartData2)
    var temp00 = areaChartData2.datasets[0]
    var temp11 = areaChartData2.datasets[1]
    barChartData2.datasets[0] = temp11
    barChartData2.datasets[1] = temp00

    var barChartOptions2 = {
      responsive              : true,
      maintainAspectRatio     : false,
      datasetFill             : false
    }

    var chart2=new Chart(barChartCanvas2, {
      type: 'bar',
      data: barChartData2,
      options: barChartOptions2
    })

	</script>

	<script>
$("#select1").on('change', function() {
  //alert( this.value );

$.ajax('/graphique2/'+this.value, {
    type: 'GET',  // http method
    success: function (data, status, xhr) {
        console.log(data);
		console.log(areaChartData.datasets);
		areaChartData.datasets[0]['data']=data[0];
		//chart1.update();

		areaChartData.datasets[1]['data']=data[1];
		chart1.update();
		
    },
    error: function (jqXhr, textStatus, errorMessage) {
            console.log('Error' + errorMessage);
    }
});
});


$("#select2").on('change', function() {
  //alert( this.value );

$.ajax('/graphique2/'+this.value, {
    type: 'GET',  // http method
    success: function (data, status, xhr) {
        console.log(data);
		console.log(areaChartData.datasets);
		areaChartData2.datasets[0]['data']=data[2];
		//chart1.update();

		areaChartData2.datasets[1]['data']=data[3];
		chart2.update();
		
    },
    error: function (jqXhr, textStatus, errorMessage) {
            console.log('Error' + errorMessage);
    }
});
});

</script>
{% endblock %}


   